name: environment variables
on: push
jobs:
  repo-name:
    strategy:
      matrix:
        REPO: [ "${{ github.event.repository.name }}"]
    steps:
    - name: "echo REpo name "
      run: |
        echo ${{env.REPO}}
     shell: bash   
      
